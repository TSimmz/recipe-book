import React from 'react';

const Navbar = () => {
  return (
    <nav>
      <h1>Recipe Book</h1>
      <ul>
        <button>Login</button>
        <button>Sign Up</button>
      </ul>
    </nav>
  );
};

export default Navbar;
